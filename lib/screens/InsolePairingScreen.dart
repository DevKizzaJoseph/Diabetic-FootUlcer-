import 'package:flutter/material.dart';

class InsolePairingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Insole Pairing'),
      ),
      body: Center(
        child: Text('Insole Pairing Screen'),
      ),
    );
  }
}